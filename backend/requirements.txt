# Dependencies are now managed via pyproject.toml at the project root.
# Use uv to install:
#
#   uv sync              # runtime + dev deps
#   uv sync --group nlp  # also install NLP training deps
#
# To generate a pip-compatible requirements file:
#   uv export --no-hashes -o backend/requirements.lock.txt
