# 心语（XinYu）后端技术说明

## 一、系统总体架构

```mermaid
graph TB
    subgraph 前端["前端（Vue 3）"]
        UI[用户聊天界面]
        Dashboard[咨询师看板]
    end

    subgraph API层["接口层（FastAPI）"]
        ChatEP["流式对话接口\nPOST /chat/{会话ID}/stream"]
        SessionEP["会话管理接口\nPOST·GET /sessions"]
        DashEP["看板数据接口\nGET /dashboard/*"]
    end

    subgraph 服务层["服务层"]
        ChatSvc[对话编排服务]
        NLPSvc[NLP 分析服务]
        RiskSvc[风险评估服务]
        CtxSvc[上下文管理服务]
        PromptSvc[提示词构建服务]
        LLMSvc[大模型接入服务]
        ResSvc[危机资源服务]
    end

    subgraph NLP层["NLP 模型层"]
        Model["多任务分类模型\n中文 RoBERTa 微调"]
        Rules["后处理规则引擎\n7 条确定性规则"]
    end

    subgraph 数据层["数据层（PostgreSQL）"]
        Sessions[(会话表)]
        Messages[(消息表)]
        Analyses[(分析结果表)]
        Summaries[(对话摘要表)]
        Alerts[(预警事件表)]
        Resources[(危机资源表)]
    end

    UI -->|HTTP / SSE| ChatEP
    UI -->|HTTP| SessionEP
    Dashboard -->|HTTP| DashEP

    ChatEP --> ChatSvc
    SessionEP --> Sessions
    DashEP --> Messages
    DashEP --> Analyses
    DashEP --> Alerts

    ChatSvc --> NLPSvc
    ChatSvc --> RiskSvc
    ChatSvc --> CtxSvc
    ChatSvc --> PromptSvc
    ChatSvc --> LLMSvc
    ChatSvc --> ResSvc

    NLPSvc --> Model
    NLPSvc --> Rules

    CtxSvc --> Sessions
    CtxSvc --> Messages
    CtxSvc --> Summaries

    ResSvc --> Resources

    ChatSvc -->|持久化| Messages
    ChatSvc -->|持久化| Analyses
    ChatSvc -->|持久化| Alerts
```

---

## 二、单轮对话处理流水线

每次用户发送消息，对话编排服务按以下顺序执行 **11 个步骤**，最终以 SSE 流式事件逐步返回结果：

```mermaid
sequenceDiagram
    actor 用户
    participant 对话接口
    participant 对话编排
    participant NLP分析
    participant 风险评估
    participant 上下文管理
    participant 提示词构建
    participant 大模型
    participant 数据库

    用户->>对话接口: 发送消息
    对话接口->>对话编排: 转发消息与会话ID

    对话编排->>数据库: 验证会话是否存在
    对话编排->>NLP分析: 分析用户消息
    NLP分析-->>对话编排: 情绪 / 意图 / 强度 / 风险辅助分
    Note over NLP分析: 模型推理 → 后处理规则校正

    对话编排->>数据库: 读取近 6 条历史预警记录
    对话编排->>风险评估: 综合评估风险等级
    风险评估-->>对话编排: 风险等级（L0–L3）与原因

    对话编排->>数据库: 更新当前会话风险等级

    对话编排->>上下文管理: 构建上下文窗口
    上下文管理->>数据库: 读取历史消息与对话摘要
    上下文管理-->>对话编排: 上下文窗口

    对话编排->>提示词构建: 组装系统提示与上下文
    提示词构建-->>对话编排: 完整提示词包

    对话编排-->>用户: SSE 元数据事件（情绪 / 意图 / 风险等级）

    alt 高风险路径（L2 / L3）
        对话编排-->>用户: SSE 预警事件（危机资源列表）
    end

    对话编排->>大模型: 流式生成回复
    loop 逐 token 输出
        大模型-->>对话编排: token
        对话编排-->>用户: SSE 文本事件
    end

    对话编排->>数据库: 写入用户消息与助手回复
    对话编排->>数据库: 写入 NLP 分析结果
    对话编排->>数据库: 写入预警事件（仅高风险）
    对话编排-->>用户: SSE 完成事件（消息ID / 耗时）
```

---

## 三、NLP 分析模块

### 3.1 多任务模型架构

本项目以 `hfl/chinese-roberta-wwm-ext` 为编码器，在其上训练了一个**多任务分类模型**。四个预测头共享编码器参数，对同一句输入同时输出四类结果：

```mermaid
graph LR
    输入文本 --> 分词器
    分词器 --> 编码器["中文 RoBERTa 编码器\n（共享参数，Dropout=0.20）"]
    编码器 --> CLS["CLS 向量（768 维）"]

    CLS --> 情绪头["情绪分类头"]
    CLS --> 意图头["意图分类头"]
    CLS --> 强度头["强度回归头（Sigmoid）"]
    CLS --> 风险头["风险辅助头（Sigmoid）"]

    情绪头 --> 情绪["7 类情绪\n焦虑 / 悲伤 / 愤怒 / 恐惧\n羞耻 / 绝望 / 中性"]
    意图头 --> 意图["6 类意图\n倾诉 / 求建议 / 求共情\n危机 / 自我披露 / 信息咨询"]
    强度头 --> 强度["情绪强度 [0, 1]"]
    风险头 --> 风险["风险辅助分数 [0, 1]"]
```

**训练配置：**

| 参数 | 值 |
|---|---|
| 基础模型 | hfl/chinese-roberta-wwm-ext |
| 训练集规模 | 3,980 条标注数据 |
| 优化器 | AdamW，学习率 2e-5，权重衰减 0.01 |
| 情绪/意图损失 | 交叉熵损失，标签平滑系数 0.10，逆频率类别权重 |
| 强度/风险损失 | 均方误差损失 / 二元交叉熵损失 |
| 学习率调度 | 前 10% 步线性预热，随后线性衰减 |

### 3.2 后处理规则引擎

模型推理完成后，规则引擎对预测结果执行**7 条确定性校正规则**，弥补模型在边界样本上的系统性混淆：

| 规则 | 触发条件 | 校正方向 |
|---|---|---|
| 规则 1 | 预测为焦虑 + 含显式恐惧词（害怕/恐惧）+ 无弥散焦虑标记 | 情绪 → 恐惧 |
| 规则 2 | 预测为恐惧 + 弥散忧虑措辞 + 无可名状具体对象 | 情绪 → 焦虑 |
| 规则 3 | 预测为羞耻 + 含外部归因词（凭什么/太不公平）+ 无自责词 | 情绪 → 愤怒 |
| 规则 4 | 预测为绝望 + 强度 < 0.62 + 无绝望语言 + 含悲伤词 | 情绪 → 悲伤 |
| 规则 5 | 含隐性危机行动框架（遗书/告别信/已经决定结束…） | 意图 → 危机 |
| 规则 6 | 预测为中性 + 含悲伤词 + 悲伤概率 ≥ 0.10 | 情绪 → 悲伤 |
| 规则 7 | 预测为自我披露 + 当下时间词 + 情绪充电词 + 倾诉概率 ≥ 0.12 | 意图 → 倾诉 |

**低置信度回退机制：** 当意图预测最高概率低于 0.50 时，系统在分析结果中标记置信度不足，提示词构建服务随即在系统提示中追加指令，要求大模型根据上下文自行判断用户意图。

### 3.3 评估结果

| 指标 | 数值 |
|---|---|
| 情绪准确率（含规则后处理）| 74.08% |
| 情绪宏平均 F1 | 74.02% |
| 意图准确率（含规则后处理）| 80.41% |
| 意图宏平均 F1 | 82.14% |
| 风险召回率 | 80.00% |
| 危机安全探针（30 条）| **30/30 全部通过** |

---

## 四、风险评估模块

风险评估服务为**纯函数式无状态模块**，综合五类信号输出 L0–L3 四级风险评估：

```mermaid
flowchart TD
    输入["当前轮分析结果 + 历史预警记录"]

    输入 --> K3{命中高危关键词？}
    输入 --> CI{意图为危机\n且风险辅助分 ≥ 0.65？}
    输入 --> HI{情绪为绝望\n且强度 ≥ 0.90？}

    K3 -->|是| L3["L3 危机\n风险分 ≥ 0.85\n触发危机响应路径"]
    CI -->|是| L3
    HI -->|是| L3

    K3 -->|否| K2{命中中危关键词 /\n风险辅助分 ≥ 0.50 /\n高危情绪高强度 /\n近期持续升级？}
    CI -->|否| K2
    HI -->|否| K2

    K2 -->|是| L2["L2 预警\n风险分 ≥ 0.55\n推送危机资源"]

    K2 -->|否| K1{风险辅助分 ≥ 0.25 /\n强度 ≥ 0.60 /\n负面情绪中等强度？}

    K1 -->|是| L1["L1 关注\n风险分 ≥ 0.30\n持续监测"]
    K1 -->|否| L0["L0 正常\n常规支持路径"]
```

**跨轮次升级检测：** 对话编排服务在每轮开始前从数据库读取近 6 条历史预警记录，若其中 ≥ 2 条为高风险（L2/L3），则判定为持续升级态势，触发 L2 预警——即使当前轮次未命中任何关键词。

---

## 五、多轮对话上下文管理

上下文管理服务实现**三层压缩架构**，解决长对话中大模型 Token 窗口有限的问题：

```mermaid
graph TB
    subgraph 第一层["第一层：原始数据层"]
        DB1[(消息表\n完整对话历史)]
    end

    subgraph 第二层["第二层：滚动摘要层"]
        Summary["对话摘要表"]
        Trigger["触发条件：消息数 ≥ 10\n此后每新增 4 条刷新一次"]
        Content["摘要内容：\n· 用户关切片段（最近 3 条）\n· 助手支持内容（最近 2 条）\n· 当前风险等级"]
    end

    subgraph 第三层["第三层：运行时窗口层"]
        Window["发送给大模型的提示词包\n= 摘要文本\n+ 摘要覆盖点之后最近 6 条消息\n+ 当前轮用户消息\n+ NLP 分析结果与风险评估"]
    end

    DB1 -->|压缩| Summary
    Summary --> Window
    DB1 -->|取摘要锚点之后的消息| Window
```

摘要以**覆盖锚点消息 ID** 标记已压缩位置，每次刷新只处理锚点之后的增量消息，避免重复计算。若锚点丢失则强制重建摘要。

---

## 六、提示词动态构建

提示词构建服务根据**风险等级**和**分类置信度**动态组装发送给大模型的系统提示：

```mermaid
flowchart LR
    风险等级 --> 提示词构建
    置信度标记 --> 提示词构建

    提示词构建 -->|L0 / L1| 标准路径["标准支持路径\n· 共情优先\n· 澄清主要压力源\n· 提供 1–2 个可行建议"]
    提示词构建 -->|L2 / L3| 危机路径["高风险支持路径\n· 立即表达关切\n· 鼓励寻求线下帮助\n· 优先告知危机资源"]
    提示词构建 -->|置信度不足| 回退指令["追加指令：\n告知模型分类置信度低\n要求根据上下文自行判断意图"]

    标准路径 --> 提示词包["完整提示词包\n= 系统提示\n+ 对话摘要\n+ 近期消息\n+ 当前消息\n+ 分析结果"]
    危机路径 --> 提示词包
    回退指令 --> 提示词包
```

---

## 七、数据库设计

系统共 8 张表，全部由数据库迁移脚本统一创建和管理：

- **第一次迁移**：建立全部 8 张核心业务表
- **第二次迁移**：为访客档案表新增 `username`、`password_hash` 两列，支持账号注册登录
- **第三次迁移**：为访客档案表新增 `real_name`、`college`、`student_id`、`is_guest` 四列；为咨询师账户表新增 `display_name`、`college` 两列，支持多学院权限隔离

```mermaid
erDiagram
    访客档案表 {
        uuid 访客ID PK
        string 用户名 "唯一，可空（游客无）"
        string 密码哈希 "bcrypt，可空（游客无）"
        string 显示名称
        string 真实姓名 "注册必填"
        string 学院 "固定枚举列表，注册必填"
        string 学号 "注册必填"
        bool 是否游客 "默认 false"
        bool 已授权
        timestamp 创建时间
    }

    会话表 {
        uuid 会话ID PK
        uuid 访客ID FK
        string 最新风险等级
        timestamp 开始时间
    }

    消息表 {
        uuid 消息ID PK
        uuid 会话ID FK
        int 序号
        string 角色
        text 内容
        string 安全模式
        int 响应延迟毫秒
        timestamp 创建时间
    }

    分析结果表 {
        uuid 记录ID PK
        uuid 消息ID FK
        string 情绪标签
        json 情绪概率分布
        string 意图标签
        json 意图概率分布
        float 情绪强度
        float 风险辅助分
        json 关键词命中
    }

    对话摘要表 {
        uuid 记录ID PK
        uuid 会话ID FK
        text 摘要文本
        int 摘要版本号
        uuid 覆盖锚点消息ID
        string 最近风险等级
        json 开放话题列表
        json 建议延续内容
    }

    预警事件表 {
        uuid 预警ID PK
        uuid 会话ID FK
        uuid 消息ID FK
        string 风险等级
        json 触发原因
        string 处理状态
        timestamp 创建时间
        timestamp 处理时间
    }

    危机资源表 {
        uuid 资源ID PK
        string 资源名称
        string 适用风险等级
        string 联系方式
        string 描述
    }

    咨询师账户表 {
        uuid 账户ID PK
        string 用户名
        string 密码哈希 "bcrypt"
        string 姓名 "可空"
        string 学院 "可空；NULL = 超级管理员"
        bool 是否启用
        timestamp 创建时间
    }

    访客档案表 ||--o{ 会话表 : "拥有"
    会话表 ||--o{ 消息表 : "包含"
    会话表 ||--o| 对话摘要表 : "拥有"
    会话表 ||--o{ 预警事件表 : "产生"
    消息表 ||--o| 分析结果表 : "对应"
    消息表 ||--o{ 预警事件表 : "触发"
```

---

## 八、咨询师看板接口

看板模块提供 12 个接口，供咨询师实时监控对话状态并管理账户权限。所有接口按调用者的学院字段自动过滤数据（见第十二章）：

| 接口 | 权限 | 功能 |
|---|---|---|
| 统计概览 | 咨询师 | 汇总总会话数、总消息数、未处理预警数、危机预警数 |
| 图表数据 | 咨询师 | 情绪标签分布（按消息聚合）+ 风险等级分布（按会话聚合）|
| 会话列表 | 咨询师 | 所有可见会话，含学生信息、消息数、主导情绪、最高风险等级 |
| 会话消息 | 咨询师 | 指定会话的全量消息，每条附带 NLP 分析结果 |
| 预警列表 | 咨询师 | 所有可见预警事件，含触发原因与处理状态 |
| 更新预警状态 | 咨询师 | 将预警状态从"待处理"更新为"已确认"或"已解决" |
| 学生档案列表 | 咨询师 | 列出所有注册学生（非游客），含学号、学院、会话数、最高风险等级 |
| 学生档案详情 | 咨询师 | 指定访客的详细信息及最近 20 条会话摘要 |
| 数据导出 | 咨询师 | 生成 Excel 文件，含"会话列表"和"预警记录"两个工作表 |
| 咨询师列表 | 超级管理员 | 查看所有咨询师账户及其状态 |
| 创建咨询师 | 超级管理员 | 为指定学院创建咨询师账户（学院从固定列表选择）|
| 切换咨询师状态 | 超级管理员 | 启用或停用学院咨询师账户（不可操作超管自身）|

---

## 九、服务组装与依赖注入

所有服务在应用启动时**一次性组装**为不可变容器，通过框架依赖注入机制传递给各路由处理函数，避免在请求处理中直接实例化服务：

```mermaid
graph TD
    配置["环境变量配置\n统一 XINYU_ 前缀\n由 Pydantic-settings 解析"] --> 容器
    数据库["数据库连接工厂\nSQLAlchemy 会话管理"] --> 容器

    subgraph 容器["服务容器（启动时构建，全局单例）"]
        上下文管理服务
        提示词构建服务
        NLP分析服务
        风险评估服务
        危机资源服务
        大模型接入服务
        对话编排服务
    end

    NLP分析服务 --> NLP备注["已配置模型路径\n→ 加载 RoBERTa 微调模型\n路径缺失或加载失败\n→ 自动降级为桩服务"]
    大模型接入服务 --> LLM备注["已配置豆包 API 密钥\n→ 连接豆包大模型\n未配置 → 使用本地模拟服务"]
```

---

## 十、身份认证系统

### 10.1 整体设计

系统针对四类用户采用差异化的认证策略：

| 用户类型 | 获取方式 | 认证方式 | 令牌有效期 | 可访问资源 |
|---|---|---|---|---|
| 游客 | 一键创建匿名档案 | 无需账号 → JWT | 24 小时 | 匿名聊天（无历史记录）|
| 注册访客 | 开放自助注册（必填真实姓名、学院、学号）| 用户名 + 密码 → JWT | 7 天 | 会话管理、聊天、历史记录 |
| 学院咨询师 | 超级管理员在后台创建 | 用户名 + 密码 → JWT | 7 天 | 本学院学生 + 匿名访客的看板数据 |
| 超级管理员 | 命令行种子脚本预置（`college` 为空）| 用户名 + 密码 → JWT | 7 天 | 全量看板数据 + 咨询师账户管理 |

### 10.2 认证流程

```mermaid
sequenceDiagram
    actor 用户
    participant 认证接口
    participant 数据库
    participant 受保护接口

    Note over 用户,认证接口: 游客路径
    用户->>认证接口: POST /auth/visitor/guest（无请求体）
    认证接口->>数据库: 创建 is_guest=true 的匿名档案
    认证接口-->>用户: JWT 令牌（有效期 24 小时）

    Note over 用户,认证接口: 注册路径
    用户->>认证接口: POST /auth/visitor/register\n{用户名, 密码, 真实姓名, 学院(枚举), 学号}
    认证接口->>认证接口: 校验学院是否在固定枚举列表中
    认证接口->>数据库: 检查用户名唯一性
    认证接口->>数据库: 写入 bcrypt 哈希密码与学生信息
    认证接口-->>用户: JWT 令牌（有效期 7 天）

    Note over 用户,认证接口: 登录路径
    用户->>认证接口: POST /auth/visitor/login {用户名, 密码}
    认证接口->>数据库: 读取密码哈希并验证
    认证接口-->>用户: JWT 令牌

    Note over 用户,受保护接口: 访问受保护接口
    用户->>受保护接口: Authorization: Bearer <令牌>
    受保护接口->>受保护接口: 验证签名、检查过期、校验角色
    受保护接口-->>用户: 200 正常响应 / 401 令牌无效 / 403 角色不匹配
```

### 10.3 JWT 结构与接口保护策略

**令牌载荷：**
```
{ "sub": "用户ID", "role": "visitor | counselor", "exp": 过期时间戳 }
```
使用 HS256 算法签名，密钥通过 `XINYU_JWT_SECRET_KEY` 环境变量注入。

**接口保护范围：**

```mermaid
graph LR
    subgraph 无需认证
        健康检查["GET /health"]
        游客登录["POST /auth/visitor/guest"]
        访客注册["POST /auth/visitor/register"]
        访客登录["POST /auth/visitor/login"]
        咨询师登录["POST /auth/counselor/login"]
    end

    subgraph 需要访客JWT
        创建会话["POST /sessions"]
        会话列表["GET /sessions"]
        查看会话["GET /sessions/{id}\n（仅本人）"]
        发送消息["POST /chat/{id}/stream\n（仅本人会话）"]
    end

    subgraph 需要咨询师JWT（结果按学院过滤）
        看板统计["GET /dashboard/stats"]
        图表数据["GET /dashboard/charts"]
        所有会话["GET /dashboard/sessions"]
        会话消息["GET /dashboard/sessions/{id}/messages"]
        预警列表["GET /dashboard/alerts"]
        更新预警["PATCH /dashboard/alerts/{id}"]
        学生档案["GET /dashboard/visitors"]
        学生详情["GET /dashboard/visitors/{id}"]
        数据导出["GET /dashboard/export"]
    end

    subgraph 需要超级管理员JWT
        咨询师列表["GET /dashboard/counselors"]
        创建咨询师["POST /dashboard/counselors"]
        切换状态["PATCH /dashboard/counselors/{id}"]
    end
```

**会话归属校验：** 访客访问自己的会话和聊天接口时，后端额外校验 `session.visitor_id == jwt.sub`，防止横向越权。

**学院归属校验：** 咨询师访问学生档案详情时，后端校验 `visitor.college == counselor.college`（超级管理员无此限制）。

---

## 十一、情绪轨迹追踪

### 11.1 问题背景

单轮 NLP 分析只能反映当前消息的情绪状态，无法感知用户在多轮对话中的**情绪变化走向**。例如：用户在第 1 轮表达极度压力（绝望），但在第 3 轮开始自我宽慰，系统的回应方式应随之调整——既不应忽视改善，也不应继续强调负面。

### 11.2 实现机制

```mermaid
flowchart LR
    subgraph 上下文管理服务
        加载情绪历史["从分析结果表\n读取近 5 条\n用户消息情绪标签"]
        计算走向["_compute_emotion_trend()"]
    end

    subgraph 提示词构建服务
        系统提示["_build_system_prompt()"]
    end

    加载情绪历史 --> 计算走向
    计算走向 -->|好转| 系统提示
    计算走向 -->|恶化| 系统提示
    计算走向 -->|稳定| 系统提示
```

### 11.3 情绪严重度映射与走向判断

每种情绪标签映射到一个 0–3 的严重度分值：

| 严重度 | 情绪标签 |
|---|---|
| 0 | 平静（中性）|
| 1 | 焦虑、恐惧 |
| 2 | 悲伤、愤怒、羞耻 |
| 3 | 绝望 |

**走向计算逻辑**（需至少 3 条历史记录）：

- 取历史序列中前（N−2）条的平均严重度 `early_avg`
- 取最近 2 条的平均严重度 `recent_avg`
- `recent_avg > early_avg + 0.5` → **恶化**
- `recent_avg < early_avg − 0.5` → **好转**
- 其余 → **稳定**

### 11.4 系统提示适配示例

| 走向 | 追加到系统提示的内容 |
|---|---|
| 好转 | 对话记录显示用户情绪已从「焦虑 → 悲伤 → 平静」逐步好转。请肯定用户的积极变化，避免过度强调负面状态，以支持其继续向好的方向发展。|
| 恶化 | 对话记录显示用户情绪持续加重（焦虑 → 悲伤 → 绝望）。请以更多耐心和关注回应，适时评估是否需要升级支持力度。|
| 稳定 | （无追加内容）|

---

## 十二、多学院权限隔离与后台管理

### 12.1 设计目标

系统定位为校园心理健康服务平台，支持多学院并行使用：各学院咨询师只能查看和导出本学院学生的数据；超级管理员（通常为学生心理健康教育中心）拥有全量数据访问权和咨询师账户管理权。

### 12.2 权限隔离逻辑

```mermaid
flowchart TD
    登录["咨询师 JWT 认证通过"]
    登录 --> 判断{counselor.college}

    判断 -->|NULL| 超管路径["超级管理员路径\n可见全部学生 + 匿名游客\n可管理咨询师账户"]
    判断 -->|非空| 学院路径["学院咨询师路径\n可见本学院学生\n+ is_guest = true 的匿名访客"]

    学院路径 --> 过滤["所有 dashboard 查询统一加过滤条件\n(visitor.college == counselor.college)\nOR (visitor.is_guest == true)"]
    过滤 --> 结果["接口返回结果、图表、导出文件\n均只含有权访问的数据"]
```

过滤逻辑在后端统一实现，前端无需感知——无论是统计卡片、图表、会话列表、预警记录还是 Excel 导出，均经过同一条件过滤，不存在数据泄露通道。

### 12.3 学院名称固定枚举

注册时用户从前端下拉菜单选择学院，不允许手动输入。原因在于：**学院字段同时作为权限边界**，若允许自由填写，拼写差异（"计算机学院" vs "计算机科学学院"）会导致权限隔离失效——咨询师的 `college` 字段和学生的 `college` 字段无法精确匹配，本该可见的学生会被漏掉。

固定枚举列表在前端（`frontend/src/constants.ts`）和后端（`backend/app/core/colleges.py`）双端维护，注册接口对传入值做服务端二次校验，传入枚举外的值返回 422 错误。

### 12.4 超级管理员后台

超级管理员登录后，看板右侧显示"管理员设置"标签页，提供两项功能：

**添加学院咨询师**：填写用户名、密码、所属学院（下拉选择）、姓名（可选），提交后立即生效。创建的账户学院字段非空，系统自动识别为学院咨询师。

**咨询师账户列表**：展示所有账户的用户名、姓名、学院、权限类型（超级管理员 / 学院咨询师）、当前状态。对学院咨询师账户可一键启用/停用（软删除设计，停用账户的令牌立即失效）；超级管理员账户不可从 UI 停用，防止误操作锁死系统。

超级管理员账户本身由服务器端种子脚本创建（`college` 字段留空），不经过任何公开接口，确保最高权限账户的创建路径与业务接口完全隔离。
